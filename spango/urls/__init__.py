def url(regex, view=None, namespace='control'):
    if namespace == 'control':
        print('默认请求')

    return_json = {
        'url': '',
        'data': '',
        'data_type': '',
    }

    return return_json
